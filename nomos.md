- [ ] (H) Support for several root dirs ::
    * To save in `.cache` and `.local` for example without having two separate `Brigid` instances
- [ ] (B) Support for markdown, text and custom :: (`Vec<u8>` & `String` = Data & filename) file types

